# Semantic Scholar API key (free): https://www.semanticscholar.org/product/api#api-key-form
# Without this, requests share a very low public rate limit and citations/
# recommendations will frequently return 429.
SEMANTIC_SCHOLAR_API_KEY=
